let fib = fn(n) {
    if (n < 2) { n } else { fib(n - 1) + fib(n - 2) }
};

let t1 = spawn(fib, 22);
let t2 = spawn(fib, 22);
let quick = spawn(fn() { return 1; });

puts(join(quick));
puts(join(t1) + join(t2));
