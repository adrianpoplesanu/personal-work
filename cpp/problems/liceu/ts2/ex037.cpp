// ts2 p18 e6
#include <iostream>
using namespace std;

int main(int argc, char * argv[]) {
    long a[3][7][5], (*x)[3][7][5], (*y)[7][5], (*z)[5], *t;

    x = &a;
    y = a; y = &a[1]; y = &(&a[1])[2]; y = &(&(&a[1])[2])[3];
    y = a; y = &*(a+1); y = &*(&*(a+1)+2); y = &*(&*(&*(a+1)+2)+3);

    z = a[1]; z = &a[1][2]; z = &(&a[1][2])[3];
    z = *(a + 1); z = &*(*(a + 1) + 2); z = &*(&*(*(a + 1) + 2 ));

    t = a[3][6]; t = &a[3][6][2];
    t = *(*(a + 3) + 6); t = &*(*(*(a + 3) + 6) + 2);

    return 0;
}
